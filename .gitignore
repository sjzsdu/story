# 运行时数据（SQLite 数据库与媒体产物）
data/

# 本地配置（可能含 API Key）
story.yaml
config.sh

# 编译产物（只忽略仓库根目录的二进制，避免误伤 cmd/story 目录）
/story
/bin/
.version-stamp

# 前端依赖与构建产物
web/node_modules/
web/dist/
web/npm-debug.log*

# 系统文件
.DS_Store
*.swp
*.swo
*~
.trae
